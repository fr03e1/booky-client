export interface BookRequest {
    // categories: number[],
    authors: number[],
    price: number[],
    year: number[],
    publishers: number[],
    pages: number,
    // sortBy: string,
    // order: string,
    // pages:string,
    // page: number
}